<% @events.each do |spree_event| %>
<li>
<%= spree_event.name %>
<%= link_to "Show", event_path(event) %>
<%= link_to 'Edit', edit_event_path(event) %>
<%= button_to 'Delete', event_path(event), :method => :delete %>
</li>
<% end %>
</ul>

<%= link_to 'New Event', 'new_spree_event' %>
