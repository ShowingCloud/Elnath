<%= form_for @review, :url => { :controller => 'reviews', :action => 'update', :id => @review } do |f| %>
<%= f.label :name, "Name" %>
<%= f.text_field :name %>

<%= f.label :description, "Description" %>
<%= f.text_area :description %>

<%= f.submit "Update" %>
<% end %>
